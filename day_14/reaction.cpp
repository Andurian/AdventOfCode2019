#include "reaction.h"


MaterialFinder::MaterialFinder(RuleSet rules) : m_rules{ rules }, m_tooMuchCreated{{}}
{
	// empty
}


int MaterialFinder::findMaterialFor(const std::string & mat, int amount)
{
	/*const auto it = m_cache.find(mat);
	if (it != m_cache.end())
	{
		const auto & entry = it->second;

	}
*/

		return 0;
}

